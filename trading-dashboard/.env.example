# Dev server proxy target for /logs and /decision-schema (default: http://127.0.0.1:3030)
# VITE_AGENT_URL=http://127.0.0.1:3030

# Optional: fetch logs directly from the agent (CORS must allow the dashboard origin)
# VITE_AGENT_ORIGIN=http://127.0.0.1:3030
